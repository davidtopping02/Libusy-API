version: '3'
services:
  api:
    image: UoD-Occupancy-Tracker-API
    build: .
    networks:
      - backend
    ports:
      - "3000:3000"

  web-cli:
    image: UoD-Occupancy-Tracker-API
    networks:
      - backend
    command: sh

networks:
  backend:
    driver: bridge
